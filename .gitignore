# Pasta de dados (dataset da Petrobras 3W)
data/
*.parquet
*.csv

# Pastas com dados brutos
3W/
data/
datasets/

# Arquivos temporários
*.tmp
*.log
__pycache__/
*.pyc

# Modelos serializados (arquivos grandes — use Git LFS se precisar versionar)
models/*.joblib
models/*.pkl
models/*.pt
models/*.h5

# Credenciais (NUNCA suba!)
.env
credentials.json
aws-keys.json
# Terraform — segredos e estado (NUNCA versionar)
terraform/*.tfvars
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.lock.hcl
terraform/override.tf
terraform/*_override.tf

# Logs de containers
logs/

# Dados gerados localmente (volume Docker)
data/processed/all_classes_combined.*

# Ambiente virtual
venv/
env/
.venv/

# VS Code (opcional)
.vscode/
