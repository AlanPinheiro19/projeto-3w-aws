# Dependencias extras instaladas no container Airflow durante a inicializacao
# Estas sao as mesmas dependencias do projeto 3W necessarias para executar
# o script etl_bronze_silver_gold.py dentro do scheduler/webserver do Airflow.

# Dados
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0

# AWS / S3
boto3>=1.34.0
s3fs>=2024.2.0

# Machine Learning
scikit-learn>=1.4.0

# Banco de dados
# sqlalchemy NAO incluido: Airflow 2.9.x gerencia sua propria versao (1.4.x)
# Incluir sqlalchemy>=2.0.0 quebraria o ORM interno do Airflow (MappedAnnotationError)
psycopg2-binary>=2.9.9
